<h1>Restaurants#index</h1>
<p>Find me in app/views/restaurants/index.html.erb</p>
 <% @restaurants.each do |x| %>
  <h1><%= x.name %></h1>
  <p><%= x.address %></p>
  <%= link_to "show", restaurant_path(x.id) %>
  <%= link_to "show reviews", restaurant_reviews_path(x.id) %>
 <% end %>

 <%= link_to "new", new_restaurant_path %>
